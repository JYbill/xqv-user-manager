export enum Role {
  Root = "Root",
  Manager = "Manager",
  Visitor = "Visitor",
}

export enum Action {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
}

export enum V1 {
  User = "/v1/user",
}
